# Academic Translator dependencies
#
# Everything here is OPTIONAL. Translating plain text (.txt) and using the
# Python API needs nothing but the standard library - install these only for
# the document formats you actually want to read.

# PDF extraction (PyMuPDF is tried first, PyPDF2 is the fallback)
PyMuPDF>=1.23.0
PyPDF2>=3.0.0

# Word document extraction
python-docx>=1.1.0
